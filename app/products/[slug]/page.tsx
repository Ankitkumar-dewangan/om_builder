import { ArrowUpRight, Check, ChevronRight } from 'lucide-react'
import Link from 'next/link'
import { notFound } from 'next/navigation'
import { getProduct, getRelatedProducts, products } from '@/lib/products'
import { InnerFooter, InnerHeader } from '@/components/inner-shell'

export function generateStaticParams() { return products.map((p) => ({ slug: p.slug })) }
export default async function ProductDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const product = getProduct(slug); if (!product) notFound(); const related = getRelatedProducts(product); return <main><InnerHeader/><section className="detail-page"><div className="breadcrumbs"><Link href="/products">Products</Link><ChevronRight size={14}/><span>{product.name}</span></div><div className="detail-grid"><div className="detail-image"><img src={product.image} alt={product.name}/></div><div className="detail-copy"><p className="eyebrow">{product.category}</p><h1>{product.name}</h1><p className="brand-line">{product.brand}</p><p className="body-copy">{product.description}</p><div className="detail-actions"><Link className="button button-green" href="/#contact">Enquire now <ArrowUpRight size={16}/></Link><a className="button button-outline-dark" href="https://wa.me/919109838902">WhatsApp</a><a className="text-link dark" href="mailto:hello@omsunbuild.com">Email us <ArrowUpRight size={15}/></a></div></div></div><div className="detail-info"><div><p className="eyebrow">FEATURES</p><ul>{product.features.map((feature) => <li key={feature}><Check size={16}/>{feature}</li>)}</ul></div><div><p className="eyebrow">APPLICATIONS</p><ul>{product.applications.map((item) => <li key={item}><Check size={16}/>{item}</li>)}</ul></div>{product.specifications && <div><p className="eyebrow">SPECIFICATIONS</p><dl>{Object.entries(product.specifications).map(([key,value]) => <div key={key}><dt>{key}</dt><dd>{value}</dd></div>)}</dl></div>}</div><section className="related"><div className="section-heading"><div><p className="eyebrow">KEEP EXPLORING</p><h2>Related<br/><em>products.</em></h2></div></div><div className="catalogue-grid">{related.map((item) => <article className="catalogue-card" key={item.slug}><img src={item.image} alt={item.name}/><div><p className="eyebrow">{item.category}</p><h3>{item.name}</h3><Link className="text-link dark" href={`/products/${item.slug}`}>View details <ArrowUpRight size={15}/></Link></div></article>)}</div></section></section><InnerFooter/></main> }
