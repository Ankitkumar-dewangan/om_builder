'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowUpRight } from 'lucide-react'
import { InnerFooter, InnerHeader, PageIntro } from '@/components/inner-shell'
import { ProjectCard } from '@/components/project-card'
import { ProjectFilter } from '@/components/project-filter'
import { InstallationStory } from '@/components/installation-story'
import { projects } from '@/lib/projects'
export default function ProjectsPage() { const [active, setActive] = useState('All'); const visible = active === 'All' ? projects : projects.filter((project) => project.type === active); return <main><InnerHeader/><PageIntro eyebrow="OM SUNBUILD · OUR WORK" title="Solar work" accent="with purpose." copy="A selection of installation work, structure and solar solutions created with care for real sites and real energy needs."/><section className="section portfolio-intro"><div><p className="eyebrow">THE WORK BEHIND THE PROMISE</p><h2>Made to be<br/><em>seen.</em></h2></div><div><p className="body-copy">From residential rooftops to larger solar infrastructure, every project starts with understanding the site and ends with a system that is ready to work.</p><Link className="button button-green" href="/#contact">Get a free quote <ArrowUpRight size={16}/></Link></div></section><section className="section projects-catalogue"><div className="section-heading"><div><p className="eyebrow">PROJECT CATALOGUE</p><h2>Our installation<br/><em>stories.</em></h2></div><p className="heading-intro">Browse the work by project type. Project details remain editable as the OM SUNBUILD portfolio grows.</p></div><ProjectFilter active={active} onChange={setActive}/><div className="portfolio-grid">{visible.map((project) => <ProjectCard project={project} key={project.slug}/>)}</div></section><InstallationStory/><section className="cta-section"><div><p className="eyebrow light">YOUR SITE COULD BE NEXT</p><h2>Let&apos;s plan your<br/><em>solar future.</em></h2></div><div className="cta-actions"><p>Tell us about your home, business or site and we&apos;ll help you understand the next step.</p><Link className="button button-light" href="/#contact">Enquire now <ArrowUpRight size={16}/></Link></div></section><InnerFooter/></main> }
